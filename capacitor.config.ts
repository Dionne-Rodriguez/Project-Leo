import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.project-leo-v2',
  appName: 'project-leo-v2',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
